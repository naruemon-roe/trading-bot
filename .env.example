# ── Copy ไฟล์นี้เป็น .env แล้วใส่ค่าจริง ──

# API Key สำหรับ Webhook (ตั้งเองได้ ยาว ๆ แบบ random string)
API_KEY=change-me-to-something-random

# MetaAPI Cloud  →  https://metaapi.cloud
META_API_TOKEN=your-metaapi-token-here
META_ACCOUNT_ID=your-account-id-here

# ── Trading ──────────────────────────────
SYMBOL=XAUUSD
LOT_SIZE=0.01
ATR_PERIOD=14
TIMEFRAME=1m

# ── ATR Multipliers ───────────────────────
# TP = entry ± (ATR × multiplier)
# SL = entry ∓ (ATR × multiplier)
TP1_MULTI=1.0
TP2_MULTI=2.0
TP3_MULTI=3.0
SL_MULTI=1.5
