# ── ตั้งค่าเหล่านี้ก่อน deploy ────────────────────────────────────────────────

# รหัสลับ — ใส่ค่าสุ่มที่เดาได้ยาก (ห้ามใช้ change-me)
API_KEY=change-me-to-something-random

# Symbol default ถ้า TradingView ไม่ส่ง symbol มา
SYMBOL=XAUUSD

# Port ที่ Flask จะ listen (5000 หรือเปลี่ยนได้)
PORT=5000

# Path ของ SQLite database (ไม่ต้องเปลี่ยนปกติ)
DB_PATH=signals.db
# Discord Webhook URL — รับแจ้งเตือนทุกครั้งที่มี signal เข้ามา
# วิธีได้: Discord → Server Settings → Integrations → Webhooks → New Webhook
# ถ้าไม่ต้องการแจ้ง Discord ให้ใส่ว่างไว้
DISCORD_WEBHOOK_URL=